apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux
  namespace: flux
spec:
  interval: 1m
  path: ./clusters/ste-test-cicd
  prune: true
  sourceRef:
    kind: GitRepository
    name: cicd-testing
    namespace: flux
